query allCurrentVotes($argAddress: String!) {
  allCurrentVotes(argAddress: $argAddress) {
    nodes {
      pollId
      optionIdRaw
      blockTimestamp
      chainId
      mkrSupport
      hash
    }
  }
}
